import { ContentEditorClient } from '@/components/admin/content-editor-client';

export default function SupportAdminPage() {
  return <ContentEditorClient initialRows={[]} contentType="support" />;
}
