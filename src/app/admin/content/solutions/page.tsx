import { ContentEditorClient } from '@/components/admin/content-editor-client';

export default function SolutionsAdminPage() {
  return <ContentEditorClient initialRows={[]} contentType="solution" />;
}
